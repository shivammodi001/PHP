<?php
    // What is MySQL?
    // 1. MySQL is a relational database management system (RDBMS) based on Structured Query Language (SQL).
    // 2. Databsd is a collection of data that is organized in a way that allows for easy access, management, and updating.
    // 3. A database is typically organized into tables, which are made up of rows and columns. Each table represents a
    // specific type of data, and each row in the table represents a single record or entry. The columns in the table represent the different attributes or fields of the data.
    // 4. Developed by oracle Corporation .

    // 1. for creating database in xampp
    // create DATABASE name_of_database;

    //2. for database list;
    // show databases;

    // 3. dor deleting database;
    // drop database name_of_database;

    // 4. for table creation;
    // create table name_of_table (column1 datatype, column2 datatype, column3 datatype, ...);

    // 5. for table list;
    // show tables;

    // 6. for deleting table;
    // drop table name_of_table;

    // 7. for inserting data into table;
    // insert into name_of_table (column1, column2, column3, ...) values (value1, value2, value3, ...);

    // 8. for updating data in table;
    // update name_of_table set column1 = value1, column2 = value2, ... where condition;

    // 9. for deleting data from table;
    // delete from name_of_table where condition;

    // 10. for selecting data from table;
    // select column1, column2, ... from name_of_table where condition;
    // select * from name_of_table; // for all data
    // select column1, column2, ... from name_of_table where condition order by column1 asc/desc; // for sorting data
    // select column1, column2, ... from name_of_table where condition limit number; // for limiting data
    // select column1, column2, ... from name_of_table where condition group by column1; // for grouping data

    //11. for joining tables;
    // select column1, column2, ... from table1 join table2 on table1.column = table2.column where condition;

    //12. for mml functions;
    // select count(column) from name_of_table; // for counting data
    // select sum(column) from name_of_table; // for summing data
    // select avg(column) from name_of_table; // for averaging data
    // select min(column) from name_of_table; // for minimum data
    // select max(column) from name_of_table; // for maximum data
    // select distinct column from name_of_table; // for distinct data
    // select column1, column2, ... from name_of_table where condition having condition; //
    // for filtering data after grouping
    // select column1, column2, ... from name_of_table where condition like '%value%'; // for searching data
    // select column1, column2, ... from name_of_table where condition between value1 and value2; // for filtering data between two values


?>